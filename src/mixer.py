# -*- coding: utf-8 -*-
def mix_tracks(track1, track2):
    mixed_track = f"{track1} + {track2}"
    return mixed_track

# Esempio di utilizzo
if __name__ == "__main__":
    mixed = mix_tracks("Track A", "Track B")
    print(mixed)
